Crawler returns array of Flat objects

const crawler = new Crawler();
let flats = await crawler.getFlats(time); //array of flatsobject

const flatChecker = new FlatChecker();
let newFlats = await flatChecker.compare(flats); //Object with array of flatsobject and filter function

const user = new User(Name, email, filter);

user.alert(newFlats.filter(this.filter));